import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chekspace.app',
  appName: 'chekspace-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
